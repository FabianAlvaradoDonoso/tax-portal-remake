export const ROOTS = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  EXECUTE_PROCESS: '/execute-process',
}

// ----------------------------------------------------------------------

export const paths = {
  home: `${ROOTS.HOME}`,
  dashboard: `${ROOTS.DASHBOARD}`,
  executeProcess: `${ROOTS.EXECUTE_PROCESS}`,
}
